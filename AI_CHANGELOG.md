# AI Change Log

## 2026-09-08 — Research progress feedback

- Request: Implement the previously discussed CLI research progress feature and explain the changed functions and runtime flow.
- Clarifications: None required; the user approved the concrete preceding proposal. Continued the user's partially pasted CLI and supervisor implementation.
- Scope: Immediate per-turn feedback, elapsed-time progress, parallel research lifecycle events, tool lifecycle events, review/follow-up events, and idle notices. Final reports remain a single complete output; no token streaming or model/prompt changes.
- Change chain: CLI input -> run_with_progress/astream(updates, custom, subgraphs=True, v1) -> scope -> supervisor_tools/run_research_task -> researcher/tool_node -> review_research -> optional follow-up -> writer -> checkpoint snapshot -> clarification or final report. Custom events travel back to CLI during execution; an independent heartbeat reports idle time.
- Files: src/mini_research_agent/cli.py consumes and formats events, manages heartbeat cleanup, and preserves the clarification thread; graphs/supervisor.py emits task/review events and cancels/awaits unfinished siblings on batch failure; graphs/research.py emits tool events while retaining ToolMessage error recovery. tests/test_progress.py covers real nested streams, errors, cancellation, parallel tasks, and the heartbeat deadline; tests/test_cli.py uses the stream/snapshot contract. Six pre-existing test modules had stale imports from before the module reorganization; their import paths were updated to allow regression testing.
- Decisions: Reused existing LangGraph streaming and checkpointer with no new dependency. Task tool-call IDs provide stable display labels. Idle deadlines follow the latest event or previous notice, avoiding a near-20-second delay after an event. The heartbeat is cancelled and awaited in finally. Research errors propagate after sibling cleanup; tool errors retain existing recovery semantics. Did not commit or push changes.
- Verification: Baseline full suite could not collect six modules due to stale imports. Initial new progress tests reproduced missing supervisor wiring/import and missing configurable heartbeat interval. Related tests then passed. Review identified the idle deadline issue; a virtual-clock regression test reproduced the 10-second-vs-1-second scheduling error before the fix. Final `.venv/Scripts/python.exe -m pytest -q`: 68 passed in 11.99s. `git diff --check` passed; Git emitted only line-ending conversion warnings. No live paid-model or external-search run was performed.
- Follow-ups: Tool events currently show tool names without their parent research-task label. Final report token streaming is outside this iteration. The idle timer depends on a responsive asyncio event loop.
